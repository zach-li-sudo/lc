class Solution:
    def searchMatrix(self, matrix, target):
        if matrix == [] or matrix[0] == []:
            return False
        
        R = len(matrix)
        C = len(matrix[0])
        

        # find row number


        # find column number





sol = Solution()
print(sol.searchMatrix(matrix, target))